# neetcode250

All the NeetCode 250 questions solved in C++/CPP.

## 📋 Overview

This repository contains solutions to all 250 NeetCode problems implemented in C++. Each solution includes:

-   **Function implementation** that can be directly used in the NeetCode IDE
-   **Runnable `main()` function** with test cases for local testing
-   **Problem explanation** (in `.txt` format where available)

## 🚀 Quick Start

### Compile and Run

To compile and run any solution:

```bash
g++ filename.cpp -o output
./output
```

**Example:**

```bash
g++ 6.group_anagrams.cpp -o group_anagrams
./group_anagrams
```

## 💡 How to Use

1. **For NeetCode IDE**: Copy the function, with the same name as used in the neetcode IDE, from the `.cpp` file (excluding `main()`)
2. **For Local Testing**: Run the entire `.cpp` file which includes test cases in `main()`
3. **For Understanding**: Read the `.txt` file for problem explanation and approach

Happy coding! 🎯
